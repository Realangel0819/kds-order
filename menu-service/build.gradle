dependencies {
    // Web, JPA, MySQL 등 서비스 구동에 필요한 라이브러리
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    runtimeOnly 'com.mysql:mysql-connector-j'
    // gRPC Server Starter (Spring Boot 3.x 호환)
    implementation 'net.devh:grpc-server-spring-boot-starter:3.1.0.RELEASE'

    // 공통 모듈에 대한 의존성 (menu.proto 생성 코드는 common에서 옮, protobuf-java/grpc-stub도 common의 api로 전이)
    implementation project(':common')
    implementation 'org.springframework.kafka:spring-kafka'

    // AWS SDK v2 DynamoDB Enhanced Client
    implementation 'software.amazon.awssdk:dynamodb-enhanced:2.20.162'
}